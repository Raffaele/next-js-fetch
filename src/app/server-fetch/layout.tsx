const ServerFetchLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      <h2>Server fetch page</h2>
      {children}
    </div>
  );
};

export default ServerFetchLayout;
