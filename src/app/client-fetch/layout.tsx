const ClientFetchLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      <h2>Client fetch page</h2>
      {children}
    </div>
  );
};

export default ClientFetchLayout;
